/**
 * Resend email client for booking notifications.
 * To be implemented when Resend is configured.
 * See .env.example for RESEND_API_KEY
 */

export async function sendBookingNotification(_data: Record<string, unknown>) {
  // Optional: send email to company on new booking
  return Promise.resolve();
}
